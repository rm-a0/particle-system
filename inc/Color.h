#ifndef COLOR_H
#define COLOR_H

class Color {
public:
    float r, g, b;

    /* Color constructor
     * -----------------
     * Parameters:
     * float r, g, b
    */
    Color(float r = 0.0f, float g = 0.0f, float b = 0.0f);
};

#endif // COLOR_H
